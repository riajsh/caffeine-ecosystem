# Someday List

Things we know about but aren't acting on yet. Nothing here needs action right now — it's here so we don't forget it, and so we remember *why* it's waiting.

---

## Before this goes live (not urgent — we're not live yet)

- ~~Move Vercel from Chris to Ria~~ — **Resolved 2026-08-19.** Live on a brand new, Caffeine-only Vercel Pro project under `hello@caffeinedaily.co`, at `https://caffeine-ecosystem-two.vercel.app`.
- ~~Set up Google OAuth for sign-in~~ — **Resolved 2026-08-19.** New Google Cloud project ("Caffeine Daily," under `hello@caffeinedaily.co`) with a working sign-in client, connected to Supabase. **Still outstanding:** Calendar API access (a separate OAuth client/scope) hasn't been set up yet — Calendar connect/sync won't work live until that's done, likely reusing this same Google Cloud project.
- ~~Move Supabase to Ria's own account~~ — **Resolved 2026-08-13, no action needed.** The Supabase project lives under Previously Unavailable's Pro account (the parent company that owns Caffeine Daily), not Chris's personal account. Ria already has access via `rs@previously.co`, so there's nothing to migrate.

## Worth a closer look together, when we're ready

- **Old engineering audit doc.** There's a file at `docs/cursor-brief-audit-fixes.md` listing about 30 issues Chris's team found in a code review (things like slow queries, a couple of small correctness bugs, some accessibility gaps). Spot-checking a few, it looks like several have already been quietly fixed by later work — but the doc was never updated to reflect that. Worth a session together to go through it, cross off what's already handled, and move any real remaining issues onto this list one at a time so they're easy to action.
- **Gmail sync.** The database is already set up for it, but the actual email-syncing feature hasn't been built (see `docs/specs/gmail-sync.md`). Only relevant once you want email history sitting alongside calendar meetings on a profile.
- ~~Eventbrite sync~~ — **Built 2026-08-19.** All phases from `docs/specs/eventbrite-sync.md` are live: connect screen (Admin → Eventbrite), event mapping (Admin → Eventbrite events), automatic attendee pull/match/tag every 30 minutes (daily for older events), and a review queue for unmatched attendees (Admin → Eventbrite review). Worth keeping an eye on for the first few real syncs to confirm it's behaving as expected — Eventbrite's platform has been unstable since the March 2026 acquisition.
- ~~Eventbrite question mapping + profile enrichment~~ — **Built 2026-08-20.** Registration-question answers (role, phone, company size) now map into profiles, with AI-assisted text cleanup and a "Possible updates" review queue for changed info on existing profiles. See BUILD-LOG entry (23) for details. Still needs an `ANTHROPIC_API_KEY` from Ria for the AI cleanup path to switch on (works fine without it via the basic fallback in the meantime).
- ~~Eventbrite workflow gaps before the 40+ event rollout (items 1-4)~~ — **Built 2026-08-20.** Sync health visibility, revoked-token detection, question-mapping reuse, and grouped/filterable review screen are all live — see BUILD-LOG entry (28). Full review at `docs/eventbrite-scaling-review.md`.
- **Eventbrite workflow — remaining lower-priority items (5-6).** From the same review: a summary count on the events list ("32 of 40 linked, 3 need attention"), and smarter bulk-linking suggestions (matching by name/date) — flagged as riskier (false matches) so left for later unless one-at-a-time linking genuinely becomes a bottleneck across the 40+ events.
- **caffeinedaily.co Directory sync.** Ria wants profiles that sign up / get listed on the public caffeinedaily.co website (Directory + Sign Up pages) pulled into Caffeine daily too, alongside Eventbrite — more names, roles, and locations to enrich the searchable database. The site is built with Lovable, which almost always means it's backed by its own Supabase project (likely separate from this app's). Whoever built the site is away until next week — once Ria has admin access to that Supabase project, come back to this: scope it out properly (probably a straightforward daily read of a `profiles`-like table there, matched/created the same way Eventbrite attendees are, reusing the same review-queue pattern) before building.
